services:
  dev:
    image: "${APP_NAME}-img:0.1"
    container_name: "${APP_NAME}-run"
    build:
      context: .
      dockerfile: ./Dockerfile
    privileged: true
    volumes:
      # AI-TOOLS
      - ../env/.pi:/home/ubuntu/.pi
      - ../env/.engram:/home/ubuntu/.engram
      # GIT-CONFIG
      - ../env/.gitconfig:/home/ubuntu/.gitconfig-volume
    ports:
      - "${APP_PORT}:${APP_PORT}"
    env_file:
      - ../.env
